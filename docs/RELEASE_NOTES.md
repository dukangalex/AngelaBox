更新说明
• 路径图按实际节点数量完整显示，仪表页可滚动，不再裁切规则列。
• 脚本改为按配置启用：编辑页和配置列表可开关，多脚本时可勾选。链式模式下脚本只改写前置配置，落地配置上的脚本不执行。
• URL 导入的脚本可在列表里「同步」上游更新。
• 修复示例脚本写入 `override_destination` 导致 sing-box 1.14 无法启动的问题。
• 链式代理说明收到右上角 ⓘ。链式模式下非中国流量必须经落地节点出口，不能直连。
