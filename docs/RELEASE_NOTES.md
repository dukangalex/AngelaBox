更新说明
• 脚本与配置覆盖不再各写一套规则。脚本开着时，隐私防护、广告拦截、中国直连、网络增强由配置覆盖开关控制，默认全开，不必改脚本代码。
• 默认脚本支持 IPv4/IPv6 双栈：国内直连含 IPv6，关闭「禁用 IPv6」后 DNS 为 prefer_ipv4 并写入 TUN IPv6。
• 分应用扫描补充常用应用名单，跳过系统资源叠加包；不再解析全部组件，降低内存尖峰。
• 简体中文界面按中国大陆用语修订（配置、应用、剪贴板），繁体中文保持台湾用词。
• 检查更新 HTTP 403 已在 1.0.55 修复。请从本页覆盖安装后，即可继续使用应用内检查更新。
• 订阅 / 脚本 / 更新 / WebDAV 仅允许公网 HTTPS；远程下载钉住所解析 IP。
• 注重隐私、安全防护、高能低耗、开箱即用。
