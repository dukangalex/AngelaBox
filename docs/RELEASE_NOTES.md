• **1.0.78：** 配置页可以长按拖动排序。Clash 导入会保留后写的策略组，以及能对上的规则集和域名通配。开脚本时 ECH 不再向「还在等 ECH 的节点」查记录，避免 `fetch ECH config list` 一直超时。国外普通域名用 fake-ip，不必先等代理上的 DNS；别人留下的 `198.18.0.0/15` 不会再被当成真实地址送出去。安全回归不再写死旧版本号，发版前必须先过这道检查。
