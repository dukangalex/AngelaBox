更新说明
• 开启脚本后，规则集按官方 1.14 用直连 HTTP 客户端下载，不再指向已删除的分组（例如 proxy-select）。
• 脚本启动失败会用中文说明原因，并自动回滚到订阅原规则加上中国直连等开关。
• 中国直连、DNS 防泄漏、广告拦截默认打开；脚本仍是每条配置单独开。
• 新建或导入配置不会自动变成当前配置。
• 中国应用排除补上银行支付等常见包名，不再把大 dex 误判成国内应用。
• 测速自动选择会踢掉不健康节点；日志不落盘、崩溃报告不再保存配置明文。
