更新说明
• 默认脚本补齐广告拦截、远控工具：均可选 REJECT-DROP / REJECT / DIRECT（远控另有国外服务），默认 REJECT-DROP。
• 地区组放到分组列表最后，前面是节点选择、自动选择和功能组。
• 链式与脚本互斥：保存链式会关掉当前配置的脚本；打开脚本会取消该配置的链式。
• 对照原覆写能力补了国内直连域名/银行应用、远控应用、STUN/TURN 与明文端口丢弃、IP 泄露检测走国外服务；镜像上不存在的规则集仍不写入。
