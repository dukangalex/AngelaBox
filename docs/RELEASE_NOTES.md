更新说明（测试版）
• 默认覆写脚本对齐更新后的机场分流：分组名为「🔰 节点选择 / ♻️ 自动选择 / 🐟 漏网之鱼」；广告拦截与远控仍可切到 DIRECT，但节点选择不提供 DIRECT；叶节点去掉 detour / dialer-proxy，避免订阅把链式带进来。
• Gemini、AI Studio、ChatGPT、Claude 走 AI 组，DNS 走远程解析，避免被国内规则误伤。国内直连域名补齐支付、家电、OEM、国内大模型等。已导入的默认脚本（revision 10 及更早）启动时会自动换成这一版。
• 内核与 1.0.59-beta / 1.0.60-beta 相同：官方 sing-box **v1.15.0-alpha.5**（本仓库发版 Go 1.25.5，fork tag `v1.15.0-chain.1`，commit `5e1593f0`）。设置 → 核心显示 `1.15.0-chain.1（官方 1.15.0-alpha.5）`。
• 「组」页每个策略组标题下仍显示当前选中节点与延迟。灰色圆点是还没测速，不是掉线。
• 本版是 **测试内核**，不是稳定版。日常使用请留在 1.0.57。要回稳定版请到 GitHub 下载 1.0.57 覆盖安装（签名相同）。
• 发行证书仍为 CN=ChainBox（SHA-256 `e7041217…`），未轮换。订阅 / 脚本 / 更新 / WebDAV 仅允许公网 HTTPS。
