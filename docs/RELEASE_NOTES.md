更新说明
• 开启脚本后启动失败：默认脚本曾写入镜像上不存在的规则集（如 geoip-fastly），内核 404 后会取消其余下载。现已只保留可用规则集；已导入的旧默认脚本会自动换成当前版本；启动时也会丢掉失效条目。链式与非链式同样适用。
• 脚本编辑改为全屏页，便于查看和修改代码。
• 示例脚本更名为「默认脚本」，脚本内容不再提及第三方客户端。
• 路径图按实际节点数量完整显示；脚本按配置启用；链式模式下脚本只改写前置。
