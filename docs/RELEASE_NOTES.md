• **1.0.79：** 工具 → 脚本 → + → 导入机场覆写。这是给本应用的脚本：地区组、服务组、自动选择、负载均衡、故障转移、TUN 和本机 127.0.0.1:17890，没有链式中转。国外普通域名用 fake-ip。不要和默认脚本绑在同一配置上。代理开着时，订阅和更新不再向隧道外的公共 DNS 查询，解析失败就停止。备份超过条数或大小会整份拒绝，不再解开一半继续恢复。发版前先跑和 CI 相同的单元测试，没过不出安装包。
