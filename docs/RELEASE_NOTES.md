• **1.0.72：** 配置页当前选用的配置会高亮（左侧色条 +「使用中」），深色主题下也能一眼看出来。默认脚本改成 Clash 系习惯：urltest 仍 10 分钟测一次，空闲 30 分钟停测，不再打断已有连接；负载均衡测地区组而不是每个节点，长时间挂机更稳、更省电。蜂窝/Wi‑Fi 能力抖动不再反复重绑网卡。本包版本为 **1.0.72-beta**。
• **1.0.71：** `geoip-cn` 等 IP 规则集「查看」会列出 CIDR（约 8000 条）。提供者页第一次解码后记住条目数，再进是秒开。
• **1.0.70：** 规则提供者「查看」直接解码本地 `.srs`；代理提供者「查看」打开配置文件，节点名自带数字时不再出现 `1  1⭐ofo`。同步/刷新不再闪白。
• **1.0.69：** 提供者「查看」改为编号条目列表。页面分「代理提供者」和「规则提供者」。仪表「配置」芯片跳到完整配置页。
• 默认脚本 overlay-revision 15：叶节点 `tcp_keep_alive` 为 `60s`；自动选择 / 负载均衡 / 故障转移均不打断已有连接。
• 内核 **v1.15.0-chain.3**（官方 1.15.0-alpha.6）。本版是测试内核。日常使用请留在 1.0.57。订阅 / 脚本 / 更新 / WebDAV 仅允许公网 HTTPS。
