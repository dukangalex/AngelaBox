Profile updates follow the running proxy instead of a pinned IP. ECH links that use a DNS name plus a DoH URL keep the query name, and websocket early-data is no longer left inside the path. Missing domain resolver "local" is filled with system DNS before start. Windows builds stay paused.
