Clash JSON, httpupgrade, and node ECH now import. xhttp and MASQUE are skipped instead of failing open. Missing TUN is filled in at runtime. The default script uses UDP DNS so it is no longer about twice as slow.
